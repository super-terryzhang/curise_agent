# 产品图片上传改造进度

更新时间：2026-09-17（Asia/Tokyo）

## 目标

从工作台进入专用的产品图片上传页面，完成逐图上传、程序校验、产品匹配、主图与显示顺序核对、批量提交、失败重试和历史恢复；默认不修改线上产品图片，只有最后确认提交才写入正式数据。

## 当前阶段

- [x] 读取 2026-09-17 生产核验、项目进度和当前 Git/GitHub 基线。
- [x] 检查工作台入口、旧批量图片页、前端 API 和单产品图片组件。
- [x] 检查产品匹配键、必须校验、主图/顺序语义、批次恢复、权限、对象存储、测试与部署约束。
- [x] 确认本次架构设计与第三步交互设计。
- [x] 写入正式设计说明和可执行实施计划。
- [x] 在隔离分支按测试驱动方式实现后端、迁移和前端。
- [x] 完成代码 review、专项测试、完整回归、类型检查与生产构建。
- [ ] 执行生产备份、数据库迁移、后端/前端发布和线上验收。

## 已确认现状

1. 工作台已有“产品图片上传”卡片，但仍指向通用数据管理页；没有专用图片工作台路由。
2. 旧流程是约 800 行的 ZIP 三步组件，刷新会丢失当前批次，且没有批次列表、恢复入口或逐行重试。
3. 产品编码在数据库中不唯一；可靠匹配必须保存 `product_id`，文件名推断只能作为受筛选范围约束的辅助匹配。
4. 旧预览只检查扩展名、大小、ZIP 路径和匹配；真实图片解码及每产品 30 张上限直到提交才失败。
5. 主图当前由 `display_order == 0` 表示；后端已有完整排序接口，但前端单产品图库没有接入拖拽排序。
6. 旧批次只保存一个 ZIP 对象；提交后删除 ZIP，因此失败行不能从服务器直接重试，也无法为刷新后的页面恢复逐图预览。
7. GCS/本地存储抽象支持逐文件上传、下载、删除和短期预览链接，5MB 单图低于 Cloud Run 请求限制，适合改成逐图暂存。
8. 工作台卡片不向财务角色展示，但旧图片接口使用包含财务角色的通用 Writer 权限；新工作台需要统一为产品上传者权限。
9. 当前 Alembic head 是 0029；CI 已覆盖后端 pytest、架构检查、前端 Vitest、TypeScript 和 Next.js build，但没有图片批量工作台前端测试。

## 当前设计决策（已确认）

- 推荐新增“直接逐图暂存”批次协议，并保留旧 ZIP API 兼容，不再让新页面依赖 ZIP。
- 第二步在正式写入前完成真实解码、格式/大小、产品匹配和容量校验；阻塞问题必须分配产品、替换文件或排除后才能继续。
- 第三步使用一个统一的最终图片网格：现有图片与待上传图片一起显示，第一张就是主图；拖动即调整最终顺序，点击“设为主图”等价于移动到第一位，所有变化在最终提交前都可重置。
- 提交时服务器再次校验产品容量和图片集合，逐行幂等写入；失败图片保留暂存文件供重试，成功图片清理暂存文件。

## 部署状态

本改造已在隔离分支实现，但尚未合并、推送或部署。当前生产仍是 2026-09-17 核验基线。

## 2026-09-17 实施记录

- 建立 `feature/product-image-upload-workbench-20260917` 隔离分支，并先记录后端 39 项、前端 89 项基线测试。
- 新增 0030 模型与迁移：直接批次来源、逐图临时对象、问题码、重试/幂等字段和每产品最终顺序计划。
- 新增直接批次领域服务与 HTTP API，覆盖创建、逐图真实解码、恢复、检查、分配/排除、计划保存、幂等提交、单行重试和 GC。
- 新增 `/dashboard/workbench/image-upload` 四步页面；第三步使用统一最终图库，第一张即主图，并支持拖动、左右移动、设为主图和恢复默认。
- 工作台卡片已改到专用路由，数据管理页移除重复入口，旧 ZIP 后端 API 暂时保留兼容，旧 800 行前端组件已删除。
- 第一轮独立代码审查发现提交幂等、图库并发、处理中取消、失败恢复、暂存清理、格式一致性和排序并发等高风险问题；已逐项整改，没有把审查意见留到生产后处理。
- 正式图片新增现在以 `source_bulk_staging_id` 作为数据库级幂等键，并在产品行锁内把正式图片、暂存状态和最终顺序按产品原子提交；同产品多行失败会作为一个完整图库计划一起重试。
- 增加活动批次唯一索引、顺序计划 revision、图库快照冲突回退、处理中禁止强制取消、卡住任务恢复、完成批次残留对象 GC、扩展名/MIME/真实格式一致性、替换文件和非所有者隔离。
- 前端补齐国家/港口筛选、保守文件名匹配、上传失败保留与重传、文件替换、处理中/中断/图库冲突状态、历史恢复、排序请求互斥及主图/顺序变化摘要。
- 当前证据：直接上传与安全专项 48 项通过；图片和产品相关扩大回归 76 项通过；前端 12 个文件 93 项通过；架构检查与改动文件 Ruff 通过；TypeScript 通过；Next.js Webpack 生产构建通过；0030 PostgreSQL upgrade/downgrade SQL 均成功生成。
- 第二轮独立复审继续发现图库冲突恢复循环、重复提交 worker、排序 revision lost update、删除/替换事务边界、筛选范围自动匹配和失败后图库变化等问题；已改为批次/排序原子 CAS，补齐恢复路径并调整事务顺序。
- 第二轮整改新增完整场景测试：冲突后重新检查并成功提交、提交执行权 CAS、错误批次与新活动批次互斥、国家/港口范围匹配、失败重试遇到图库变化后回到核对。
- 整改前完整后端回归为 1728 passed / 94 skipped；整改后专项 64 项、前端 93 项、TypeScript、Webpack build、架构检查和 Ruff 均通过，最终完整回归将在合并前重新执行。
- 终审进一步把取消、上传、校验、分配、替换、排序、恢复和 worker 全部纳入同一批次行锁；活动批次唯一索引覆盖 direct 与兼容 ZIP 流程，避免任何路径覆盖 `processing`。
- 自动匹配现在使用页面当时可见的完整候选产品 ID（含关键词与有效状态），失败重传保存原筛选快照；图库变化后的失败重试会强制返回第三步，并由服务端确认接口解除提交门禁。
- 最新证据：后端完整回归 1732 passed / 94 skipped；终审整改后图片专项 65 项、前端 93 项、TypeScript、架构检查、Ruff 与 0030 PostgreSQL upgrade SQL 均通过。终审整改后的最终完整回归仍待最后一次执行。
- 行锁终审后又将 GC 纳入相同的行锁与状态/时间二次检查，避免陈旧扫描覆盖 worker；刷新恢复也会把待复核计划直接送回第三步，并优先显示图库冲突的重新检查操作。
- 行锁版本完整回归为 1733 passed / 94 skipped；最终两处局部修正后后端专项 52 项、前端 93 项、TypeScript、架构检查与 Ruff 通过，发布前将再跑最后一次完整回归。
- 最后一次完整回归暴露出原有询价测试在 SQLite `StaticPool` 单连接上并发提交的随机失败；生产 PostgreSQL 不受影响。现已只在 SQLite 下把询价 worker 串行化，并增加“SQLite 为 1、PostgreSQL 保留请求并发数”的确定性保护测试。
- 最终独立复审没有发现 Critical/Important 问题；后端最终完整回归为 1734 passed / 94 skipped / 0 failed，询价专项 144 项、图片专项 76 项通过。
- 前端最终验证为 12 个文件、93 项测试通过，TypeScript 无错误，Next.js Webpack 生产构建成功；架构检查 0 违规，0030 PostgreSQL upgrade/downgrade SQL 均成功生成并人工确认活动批次唯一索引及回滚语句。

## 当前发布状态

代码仍只在隔离 feature branch，尚未合并、推送或部署。代码复审和发布前验证已完成，下一步是合并后标准生产构建、推送、生产备份/迁移、后端与前端发布及线上验收。
