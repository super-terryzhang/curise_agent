# 单位换算规则管理界面生产核验（2026-09-24）

## 最终状态

- 源码：`main@c50f322`（完整提交 `c50f3221c7b1e80fe4afda5657ab194b146d1b32`）。
- 前端：Vercel Production `dpl_D1ArQ9TcAjCyXQi38Apy38hEScJr`，状态 `Ready`，正式域名已绑定。
- 正式入口：`https://cruise-v3-frontend.vercel.app/dashboard/data?tab=unit-conversion-rules`。
- 数据库仍为 `0031_unit_conversion_rules`；后端、Cloud SQL、Oracle Job 和 Scheduler 均未修改。

## 本次快速迭代内容

- 数据管理新增“单位换算规则”页，集中展示全部、草稿、已审核和已停用数量。
- 列表支持按状态、适用范围、单位、商品编号和业务依据筛选，并显示精确换算关系、有效期间、版本和最后更新信息。
- 详情面板展示当前数据库快照：规则作用域、商品/包装、拆包判断、业务依据、创建/更新/审核人员编号与时间，以及当前修订版本。
- 明确提示草稿不会自动生效，并如实说明当前表不保存每次修改前的完整历史快照。
- 管理员可以在填写可复核依据并确认后审核或停用规则；新规则仍必须从具体 PO 商品行登记。

## 验证证据

- 针对性测试：3 个文件、7 项测试通过，并在实现前确认缺失接口、视图模型和页面组件会失败。
- 前端完整测试：20 个文件、`123 passed`。
- TypeScript：`pnpm exec tsc --noEmit` 通过。
- 生产构建：`pnpm build` 通过，16 个静态页面全部生成；最初沙箱内构建因禁止 Turbopack 绑定本地端口失败，在获准的构建环境中以相同命令成功。
- 正式域名核验：Vercel inspect 确认正式别名指向上述 Ready deployment；新数据页匿名只读请求返回 HTTP 200。

## 边界与回退

- 本次没有执行数据库迁移、生产数据写入、后端发布或 Oracle 扫描。
- 自动化没有替用户审核、停用任何生产规则；生产原有 5 条候选仍需用户在正式账号内核对。
- 如需回退，只需重新提升上一前端 deployment `dpl_8VjLndA9QmwgeojV7mZ65bfZmSy4`；后端和数据库不需要回退。
- 最终交互验收由用户登录后完成：检查筛选、详情、草稿提示，并仅在具有真实业务证据时测试审核或停用操作。
